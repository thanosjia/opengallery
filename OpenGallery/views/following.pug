doctype html
html(lang='en')
    head 
        title Open Gallery
        link(rel="stylesheet", href="/styles.css")
    body 
        header 
            nav 
                a(href='/') Home
                if loggedIn
                    a(href='/logout') Logout
                    a(href='/user/' + loggedInUserId) Profile
                else
                    a(href='/login') Login
                    a(href='/user') Sign Up
                a(href='/workshops') Workshops
                a(href='/search') Search Gallery
        h1 Following
        ul.link-list
            each followingUser in following
                li
                    a(href=`/user/${followingUser._id}`) #{followingUser.username}